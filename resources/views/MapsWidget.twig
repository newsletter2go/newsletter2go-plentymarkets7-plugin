{% if source is not empty %}
    <div>
        <iframe src="{{ source }}" width="{{ width }}" height="{{ height }}" frameborder="0" style="border:0" ></iframe>
    </div>
{% else %}
    <h1>Fehler!</h1>
{% endif %}